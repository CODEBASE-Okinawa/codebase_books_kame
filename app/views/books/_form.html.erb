<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with(model: @book) do |f| %>
      <%= f.label :title %>
      <%= f.text_field :title %>

      <%= f.label :author %>
      <%= f.text_field :author %>

      <%# bookモデルにstockカラムを追加した後に表示させる。 %>
      <%# <%= f.label :stock %>
      <%# #<%= f.number_field :stock %>    

      <%= f.submit "本を登録する", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>